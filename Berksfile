# -*- mode: ruby -*-
source 'https://berks.vandelay.io/'

metadata

# Undo for local testing
group :test do
#    cookbook 'balanced', path: '../balanced'
#    cookbook 'precog', path: '../precog'
end
